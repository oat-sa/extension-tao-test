define('taoTests/test/runner/runnerComponent/mockPlugin', ['taoTests/runner/plugin'], function (pluginFactory) {
    'use strict';

    return pluginFactory({
        name: 'mock',
        init: function () {
            var self = this;
            setTimeout(function() {
                self.trigger('loaded');
            }, 250);
        }
    });
});

define('taoTests/test/runner/runnerComponent/mockProvider', [], function () {
    'use strict';

    return {
        name: 'mock',
        init: function () {
            var self = this;
            setTimeout(function() {
                self.trigger('mock-provider-loaded');
            }, 250);
        },
        loadAreaBroker : function() {

        }
    };
});
